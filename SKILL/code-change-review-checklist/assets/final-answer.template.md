<!--
最終回答の雛形。【】の部分を実際の値に置き換え、該当しない任意の節は行ごと削除する。
文体は、ユーザーが指定した言語に合わせる(この雛形は日本語)。
必須の節: 出力先 / 結果 / 検証 / 注意。任意の節: 入力の扱い(入力を差し替えた・絞った・作り直した場合に必須)、確認できていないこと。
-->

【対象】(例: 直近1コミット e53136d1)のレビュー確認リストを作成しました。

**出力先**
`【出力したHTMLのパス】`
【同じ場所に置いた他のファイル(元のレポート、risk-scores.json など)があれば1行で】

**入力の扱い**(入力を差し替えた・絞った・作り直した場合は必須。無ければ節ごと削除)
- 【何を入力にしたか。例: `review-data.json` は2コミット分で古かったため、検証済みの `…/review.json` を入力にした】
- 【元のHTMLを作り直した場合はその旨と、使った生成スクリプト】
- 元の `review-data.json` とレポートHTMLは変更していない。

**結果**
- テーブル数: 【N】件(変更あり【A】、変更なし【B】)。
- 帯ごとの件数: 最優先【n1】、高【n2】、中【n3】、低【n4】。
- 点数の高い上位3件:

  | 点数 | 位置 | 理由 |
  |---|---|---|
  | 【点数】 | `【ファイルパス:開始行-終了行】`(【stepId】) | 【reason をそのまま、または1文に要約】 |
  | 【点数】 | `【位置】`(【stepId】) | 【reason】 |
  | 【点数】 | `【位置】`(【stepId】) | 【reason】 |

  【上位3件と同点の件があれば1行で触れる。例: 4件目の `…`(5点)も同じ変更で、中の帯に入っている】

**確認できていないこと**(任意。`checkPoints` に書いた「〜か」のうち、この作業で確認しきれなかった重要なもの)
- 【例: 復元候補の採用時に採番が同期されるか。既定値までは確認したが、呼び出し先の実装は未確認】

**検証**
- 採点の検証: `Risk score self-test passed` / `Risk score validation passed: 【N】 tables`。
- 部品の検証: `Template verification passed`。
- 生成: `Checklist verification passed: 【N】 tables, original HTML unchanged, scores reflected`。
- サーバーは起動していない。

**注意**
- 点数は調査担当の判断であり、低い点数(0〜2)は「人が見なくてよい」ことを保証しない。
- 確認済みの状態はそのブラウザの `localStorage` にだけ保存され、別の端末やブラウザとは共有されない。
- 【ブラウザでの目視確認をした場合: 確認した内容(パネルの並び順、チェックで進捗が変わること)。していない場合: 「ブラウザでの目視確認は未実施。開いて確認してほしい」と理由(例: パスに全角文字を含む)】
